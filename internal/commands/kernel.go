package commands

import (
	"github.com/velocitykode/velocity"
)

// Register declares all custom commands for the application.
// Add your commands here - generated commands (via `vel make:command`)
// should be registered in this function.
func Register(r *velocity.Commands) {
	// r.Add(&GreetCommand{})
}
